<%= form_for(@campaign) do |f| %>
  <% if @campaign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campaign.errors.count, "error") %> prohibited this campaign from being saved:</h2>

      <ul>
      <% @campaign.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ratio_random %><br>
    <%= f.number_field :ratio_random %>
  </div>
  <div class="field">
    <%= f.label :ratio_weighted %><br>
    <%= f.number_field :ratio_weighted %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
